---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---
<div class={`container ${className}`.trim()}>
  <slot />
</div>

<style>
  .container {
    max-width: var(--container-max);
    margin: 0 auto;
    padding: 0 1.5rem;
  }
</style>
